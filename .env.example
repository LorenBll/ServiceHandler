# ServiceHandler Configuration
# Copy this file to .env and fill in the values for your environment.

# Path to the Fernet encryption key file (resolved via DiskIdentifier).
# Required for API key operations.
# API_KEY_STORE_KEY_PATH=

# JSON object mapping service names to cipher-encrypted API keys.
# Loaded at session initialization.
# SH_API_KEYS={}
